name: AI factory implementation

on:
  issues:
    types: [labeled]
  workflow_dispatch:
    inputs:
      issue_number:
        description: Existing authorized issue number
        required: true
        type: string

jobs:
  implement:
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.label.name == 'ai:build' }}
    permissions:
      actions: write
      contents: write
      issues: write
      pull-requests: write
      statuses: write
    uses: shanchaudary/shans-ai-software-factory/.github/workflows/reusable-implement.yml@03ad917f85c5b72ace5b5caf225f1752e59d428d
    with:
      issue_number: ${{ github.event_name == 'workflow_dispatch' && fromJSON(inputs.issue_number) || github.event.issue.number }}
      factory_ref: 03ad917f85c5b72ace5b5caf225f1752e59d428d
    secrets:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
