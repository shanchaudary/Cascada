name: AI factory supervisor

on:
  workflow_run:
    workflows: ["Cascada CI"]
    types: [completed]
  workflow_dispatch:
    inputs:
      ci_run_id:
        description: Existing completed consumer CI workflow run ID
        required: true
        type: number

jobs:
  supervise:
    permissions:
      actions: write
      contents: write
      issues: write
      pull-requests: write
      statuses: write
    uses: shanchaudary/shans-ai-software-factory/.github/workflows/reusable-supervise.yml@3eef9707c3cd4a109925f31639df0d9ecd175c32
    with:
      ci_run_id: ${{ github.event_name == 'workflow_dispatch' && inputs.ci_run_id || github.event.workflow_run.id }}
      factory_ref: 3eef9707c3cd4a109925f31639df0d9ecd175c32
    secrets:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      ZAI_API_KEY: ${{ secrets.ZAI_API_KEY }}
